@model IEnumerable<RepositoryPatternDemoDone.Models.Neighbourhood>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NeighbourhoodGroup)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Neighbourhood1)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Neighbourhood1">Details</a> 
            </td>
        </tr>
}
    </tbody>
</table>
